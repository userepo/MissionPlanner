[package]
name = "dflog-ffi"
version = "0.7.1"
edition.workspace = true
license.workspace = true
description = "C ABI for dflog-core, consumed by Mission Planner over P/Invoke"

[lib]
name = "dflog_ffi"
crate-type = ["cdylib"]

[dependencies]
dflog-core = { path = "../dflog-core" }

[lints]
workspace = true
